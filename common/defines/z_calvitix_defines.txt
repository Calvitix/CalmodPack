#Added by Calvitix
NAI = {
		#AI_FREE_JOBS_DISTRICT_BUILD_CAP = 2 #5		#1	# AI will not build new districts on a planet with more than this many free jobs
		#AI_UPGRADE_SCORE_MULT = 80				#40	# AI will score building upgrades this much more ( since they don't take up a new building slot )		
		
		#BUILDING_DEMOLISH_THRESHOLD = -0.9	# At AI weight less than or equal to this, demolish building or district
		#BUILDING_BUILD_THRESHOLD = 0.9 #1 Calvitix		# At AI weight greater than or equal to this, the AI will consider building it.
		
	}