wg_claim_subject = {
	casus_belli = cb_buffer_zone

	hide_if_no_cb = yes

	allowed_peace_offers = {
		status_quo
		surrender
		demand_surrender
	}

	surrender_acceptance = -150

	potential = {
		has_total_war_cb = no
	}

	possible = {
		always = yes
	}

	on_accept = {
		event_target:chosen_border_subject = {
			set_subject_of = {
				who = root
				subject_type = vassal
			}
		}
	}

	on_wargoal_set = {
		save_event_target_as = buffer_zone_mother
		if = {
			limit = { 
				NOT = { exists = event_target:chosen_border_subject }
			}
			from = {
				country_event = { id = veru.6000 }
			}
		}
		country_event = { id = veru.6200 days = 5 }
	}

	ai_weight = {
		weight = 100
	}
}
